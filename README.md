# preemptive-priority-cpu-scheduling
C implementation of Preemptive Priority CPU Scheduling algorithm where higher priority values indicate higher priority. The program calculates waiting time, turnaround time, and their averages for given processes.
